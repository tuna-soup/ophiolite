---
title: Derived Asset
description: Output produced from other assets through an operator.
draft: false
---

A **Derived Asset** is an output that records how it was produced.

That matters because the value is not only the data. It is also the repeatable path from source assets, operator version, and parameter values to the output you now depend on.
