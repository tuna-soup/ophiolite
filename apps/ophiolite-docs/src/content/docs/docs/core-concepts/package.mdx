---
title: Package
description: The persisted storage unit for an asset or installable component.
draft: false
---

A **Package** is the persisted storage unit behind an asset or installable component.

For data assets, packages keep bulk payloads and metadata in inspectable local formats. For operators, packages carry manifests, entrypoints, and compatibility declarations.

Projects organize work. Packages store the concrete units inside that work.
