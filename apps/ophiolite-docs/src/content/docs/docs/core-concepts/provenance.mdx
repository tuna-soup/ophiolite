---
title: Provenance
description: The execution history attached to derived outputs.
draft: false
---

**Provenance** is the execution history attached to a derived output.

In Ophiolite, provenance should record enough information to answer:

- which operator ran
- which package or provider supplied it
- which inputs were used
- which parameters were chosen

That history is a product feature, not extra bookkeeping.
