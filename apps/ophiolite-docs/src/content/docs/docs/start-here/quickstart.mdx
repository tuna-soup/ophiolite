---
title: 10-Minute Quickstart
description: Get a first success with the current public Ophiolite surfaces.
draft: false
---

**Audience:** first-time builders  
**Status:** Preview

This quickstart is designed around the public surfaces that exist today. It proves the local runtime, interface split, and core nouns without pretending every workflow is equally mature yet.

## 1. Create a project

```python
from ophiolite_sdk import Project

project = Project.create("demo-project")
summary = project.summary()
print(summary.root)
print(summary.well_count)
print(len(project.wells()))
```

## 2. Inspect a source LAS file

```powershell
cargo run --quiet --manifest-path Cargo.toml -p ophiolite-cli -- inspect-file .\examples\sample.las
```

This is the current fastest way to validate that the runtime sees the source artifact correctly.

## 3. Materialize a standalone package

```powershell
cargo run --quiet --manifest-path Cargo.toml -p ophiolite-cli -- import .\examples\sample.las .\demo-log.laspkg
```

## 4. Inspect the package

```powershell
cargo run --quiet --manifest-path Cargo.toml -p ophiolite-cli -- summary .\demo-log.laspkg
cargo run --quiet --manifest-path Cargo.toml -p ophiolite-cli -- list-curves .\demo-log.laspkg
```

## 5. Inspect project wells from Python

```python
from ophiolite_sdk import Project

project = Project.open("demo-project")
print(project.summary().well_count)
print(len(project.wells()))
```

## What you proved

- the Rust runtime is installed
- Python can create and query a local project
- Python speaks in project and well-domain nouns first
- the CLI can inspect and package source data
- advanced platform and operator surfaces exist without defining the beginner story

## What comes next

If you want a builder workflow, continue to [Build Workflows](/docs/build-workflows/). If you want extensibility, go straight to [Operators](/operators/).
