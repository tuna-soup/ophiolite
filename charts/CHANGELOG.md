# Changelog

## Unreleased

- narrowed `@ophiolite/charts` root exports to the launch chart families
- added `@ophiolite/charts/preview` and `@ophiolite/charts/extras` subpaths
- added `@ophiolite/charts/adapters/ophiolite` as the explicit Ophiolite integration surface
- introduced neutral public seismic models and callback payloads
- added the separate `apps/public-docs` app for the public learning surface
- added canonical docs/example inventory for the launch families
- documented benchmark methodology and migration guidance
